import { ContactForm } from '@/components/contact-form';
import { ContentContainer } from '@/components/content-container';

export default function ContactPage() {
  return (
    <ContentContainer title="Contact Alora">
      <ContactForm />
    </ContentContainer>
  );
}
